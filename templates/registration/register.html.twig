{% extends 'base.html.twig' %}

{% block title %}Registrierung - RSS Reader{% endblock %}

{% block body %}
<div class="row justify-content-center">
    <div class="col-md-6 col-lg-4">
        <div class="card shadow-sm">
            <div class="card-header bg-primary text-white text-center">
                <h3 class="mb-0">
                    <i class="bi bi-person-plus me-2"></i>
                    Registrierung
                </h3>
            </div>
            <div class="card-body">
                <p class="text-muted text-center mb-4">
                    Erstellen Sie ein neues Konto für RSS Reader
                </p>

                {{ form_start(registrationForm) }}
                    <div class="mb-3">
                        {{ form_label(registrationForm.email) }}
                        {{ form_widget(registrationForm.email) }}
                        {{ form_errors(registrationForm.email) }}
                    </div>

                    <div class="mb-3">
                        {{ form_label(registrationForm.plainPassword.first) }}
                        {{ form_widget(registrationForm.plainPassword.first) }}
                        {{ form_errors(registrationForm.plainPassword.first) }}
                    </div>

                    <div class="mb-3">
                        {{ form_label(registrationForm.plainPassword.second) }}
                        {{ form_widget(registrationForm.plainPassword.second) }}
                        {{ form_errors(registrationForm.plainPassword.second) }}
                    </div>

                    <div class="mb-3">
                        {{ form_widget(registrationForm.submit) }}
                    </div>
                {{ form_end(registrationForm) }}

                <div class="text-center mt-3">
                    <p class="text-muted">
                        Bereits registriert? 
                        <a href="{{ path('app_login') }}" class="text-primary">Hier anmelden</a>
                    </p>
                </div>
            </div>
        </div>

        <div class="card shadow-sm mt-3">
            <div class="card-body">
                <h6 class="card-title text-primary">
                    <i class="bi bi-info-circle me-2"></i>
                    Registrierungshinweise
                </h6>
                <ul class="mb-0 text-muted small">
                    <li>Nach der Registrierung erhalten Sie eine Bestätigungs-E-Mail</li>
                    <li>Klicken Sie auf den Link in der E-Mail, um Ihr Konto zu aktivieren</li>
                    <li>Das Passwort muss mindestens 6 Zeichen lang sein</li>
                    <li>Ihre Daten werden sicher verschlüsselt gespeichert</li>
                </ul>
            </div>
        </div>
    </div>
</div>
{% endblock %}